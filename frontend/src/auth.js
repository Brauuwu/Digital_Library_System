// auth.js
import AuthProvider, { 
  AuthContext, 
  getToken, setToken, 
  getUser, setUser, 
  logout 
} from './auth.jsx';

export {
  AuthProvider,
  AuthContext,
  getToken,
  setToken,
  getUser,
  setUser,
  logout
};

export default {
  getToken,
  setToken,
  getUser,
  setUser,
  logout
};
